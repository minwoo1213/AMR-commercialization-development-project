LED_Toggle_Control(1, 3,100,3,100);
LED_Turn_On(63);
//100으로 키고잇기
-----------------------------------------------------------------------------------------------------------------
m_bFlag_Obstacle_cygbot 전부

 _pFlag_Value.m_bFlag_Obstacle_Left_b 와
_pFlag_Value.m_bFlag_Obstacle_Right_b 로 변경

printf("Detected laser scan behind obstacle !! \n");
-----------------------------------------------------------------------------------------------------------------
11 ~ 16 lift 전까지 도킹모듈 (시작 전에 roslaunch tetra_2dnav ar_marker_20.launch)

15끝나면 18보내서 break;

16은 lift 완료 시 주면 되는 내용으로 1.5m 리프트 기준 원점으로 후진

TODO.... 16끝났을때 lift 연동하기 및 도킹 + LIFT 도킹 스위칭 방법 개발
-----------------------------------------------------------------------------------------------------------------
1. 로봇 전원 ON 시 원점(충전스테이션)에서 시작, set_marker10_cmd 관제에서 내려야 함.

//GetInformation_Command

날렷을 때 'ex_iLiftMode' 의 값 (Lift) 정의:

0 = 정상상태 대기상태
1 = 리프트 중인 상태
9 = 리프트 동작 도중 문제가 생긴상태

10 = 마커사이즈 10으로 설정
20 = 마커사이즈 20으로 설정
------------------------------------------------------------------------------------------------------------------

Patrol_lift_Command -> 이게 관제에서 날리는 command임 원래 conveyor에서 쓰던거 다 똑같이 만듦. 명령에 conveyor만 lift로 다 바꾸면 됨

docking_cmd marker id, mode : 11 -> 일반 리프트 UP
docking_cmd marker id, mode : 21 -> 일반 리프트 DOWN

// 항상 도킹 가능한 위치 : LIFT 포인트 앞에서 실행해야 함